name: Daily Job Search

on:
  schedule:
    - cron: '7 7 * * *'
      timezone: 'America/Vancouver'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  job-search:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Ensure history directory exists
        run: |
          mkdir -p .job_history
          if [ ! -f .job_history/sent_jobs.json ]; then
            echo "[]" > .job_history/sent_jobs.json
          fi

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run job search script
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECIPIENT: ${{ secrets.EMAIL_RECIPIENT }}
          SCRAPERAPI_KEY: ${{ secrets.SCRAPERAPI_KEY }}
          RESUME_CONTENT: ${{ secrets.RESUME_CONTENT }}
          LATITUDE: ${{ secrets.LATITUDE }}
          LONGITUDE: ${{ secrets.LONGITUDE }}
        run: |
          python job_search.py

      - name: Commit sent job history
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .job_history/sent_jobs.json
          git diff --cached --quiet || git commit -m "Update sent job history"
          git push

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: job-search-logs-${{ github.run_number }}
          path: |
            *.log
            jobs_*.json
            .job_history/sent_jobs.json
          retention-days: 30
          if-no-files-found: warn
